name: CI

on:
  push:
    branches: [master]
  pull_request:
    branches: [master]

jobs:
  ci:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Checkout bt_sdk
        uses: actions/checkout@v4
        with:
          repository: andytam101/bt_sdk
          path: bt_sdk
          token: ${{ secrets.GH_PAT }}

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install bt_sdk
        run: pip install -e bt_sdk

      - name: Install dependencies
        run: pip install -e ".[dev]"

      - name: Format check
        run: |
          ruff format --check backtest.py
          ruff check backtest.py

      - name: Type check
        run: mypy backtest.py

      - name: Test
        run: pytest backtest.py